# TCW-AUD independent audit report (verbatim; defects 1-4 corrected in-wave post-audit)

> Extracted verbatim from the lane agent's final report.

All evidence gathered. The mid-audit commit is docs-only (evidence pack), so every gate and source finding remains valid. Compiling the final report.

---

# TCW-AUD — INDEPENDENT AUDIT REPORT — Tender Functional Closure Wave

**Tree:** `C:\ANtigravity Apps\Hala App - Amin review\.tcw-trees\integration`, baseline `93799d3` (tag `tender-closure-start` — verified).
**Audit note on HEAD:** the brief said HEAD = `0035872`. At 11:12:02 (while my gates were running against `0035872`) a further commit `9862eab` landed in this tree — **docs-only** (adds 10 files under `docs/tender-closure/`: 5 evidence registers + 5 lane reports; `git diff 0035872..9862eab --name-status` shows zero source files). All source-level findings, gates and probes are therefore valid for both commits. Nothing was modified by me anywhere; live DB access was read-only anon GET; the key was never printed.

## Per-item verdicts

**1. GATES — CONFIRMED.** Re-run myself in the tree:
- `pnpm check` (tsc --noEmit): exit 0, **0 `error TS` lines**.
- `npx vitest run`: exit 0, **Test Files 67 passed (67), Tests 943 passed (943)**, duration 7.36s. No flaky final-pack-fidelity teardown error appeared, so no re-run was required.
- `pnpm build`: exit 0, **"2494 modules transformed"**, built in 23.07s.
All three claimed numbers (tsc 0 · 943 tests / 60+ files · 2,494 modules) reproduce exactly.

**2. CORRECTION A — CONFIRMED.** The three ops are genuinely functional against `type_details.submission_readiness`:
- Service: `updatePlaceholderStatus`/`updateRequiredDocStatus`/`updateComplianceStatus` (supabase-tender-actions.ts:1496/1521/1544) → shared `updateSubmissionReadinessItem` (:1432): missing/blank id refused (:1445-1447), status validated against the section union (:1448-1451), exact-id targeting with honest "no item with id … Nothing was changed" (:1457-1463), sibling rows byte-preserved (:1467-1471), write via `saveTenderSourceRecord` with `expectedRevision` (:1473-1480), read-back comparison of stored rows (:1350-1369), audit outcome via `savedWithAuditOutcome` (:1483-1490).
- Store: DB-level guard `.eq('updated_at', expectedRevision)` (supabase-tender-source-record.ts:51) + id/ticket_type/active targeting (:48-50); zero-row disambiguated into honest 'stale' (row returned, nothing written) vs 'failed' (:58-65).
- Disabled ops stay disabled: `updateGateStatus` :1569-1579, `logMockBypass` :1584-1592, `updatePackStatus` :1263-1272 all return `disabledLegacyTenderChildWrite`; `logEmailSimulation` :1621-1628 returns failure. `createActivityNote` :1603-1616 → awaited, id-confirmed insert (`writeCanonicalTenderAuditConfirmed` :203-223).
- Reachability: stage-9 tab list carries "Placeholders", "Required Documents", "Compliance" (tender-stage-config.ts:335); `toCleanTabId` (TenderWorkspace.tsx:1491-1493) slugs them to exactly the ids routed at TenderWorkspace.tsx:2067-2069; the stage-gated `final_approved` branch (:2002) does not intercept them. The tabs are real managers, not shells: TenderPlaceholdersTab.tsx is full CRUD + per-row status with a tab-owned confirmed read (`useSubmissionReadinessRegister` :110-141), honest three-state load (:57-101), stale-preserving retry (:553-554), amber audit-warning rendering (:187-204); the two siblings consume the same foundation with their own per-item ops (TenderRequiredDocumentsTab.tsx:84-92; TenderComplianceMatrixTab.tsx:58-67).

**3. CORRECTION B — PARTIAL (fixed as mandated; residue found by sweep).**
- The four named suspects are fixed: B1 derives from `final_approved.approval_record.decision` with "Not recorded" fallback (tender-stage-config.ts:305-328); B2 "Version Integrity: Not recorded" (:328); B3/B4 internal-review states derive from per-block `<dept>_status` with "Not recorded" (:240-279). B5-B7 qualification gauges derive-or-"Not assessed" (:136-166). B23 TenderOverview "On Track" now requires at least one captured input, else "Not enough data" (TenderOverview.tsx:150-158).
- `saved={true}` sweep: 11 surviving sites, all carrying justified read-only-projection/no-draft-interval comments (ApprovalMatrixStage.tsx:637,730,797; FinalApprovedStage.tsx:387,638,824; CommercialVolumeTab.tsx:79; TechnicalVolumeTab.tsx:79; InternalReviewDashboardTab.tsx:139; InternalReviewExceptionsTab.tsx:113; SubmittedStage.tsx:192) — spot-verified true (CommercialVolumeTab/InternalReviewDashboardTab have zero data inputs; SubmittedStage's editable views get real dirty wiring, :89-146).
- Residue (defects list below): the Global Intelligence drawer's Deadline tile fabricates "Overdue" when no deadline is captured; the dormant `indicators` array still contains pack-derived "Strategic Fit"/"Feasibility"/"Scope Clarity" gauges (B7-census-named) and a 999-day sentinel; "All Clear" borderline in the Exceptions tab.

**4. CORRECTION C — CONFIRMED.** Verified 5 writers across lanes:
1. *Register op* (T5/T1): `updatePlaceholderStatus` — full trace in item 2; UI threads `expectedRevision` from the tab's confirmed read (TenderPlaceholdersTab.tsx:385,541).
2. *Facet patch-merge* (T3): `updateTenderSolutionDesignData` → `patchCanonicalTenderFacet` (`{...currentFacet, ...patch}`, supabase-tender-actions.ts:341-386); SolutionConfigurationTab sends only `{configuration}` with `expectedRevision` via `runTenderTabSave`/`tenderRevisionTokenOf(ws)` (SolutionConfigurationTab.tsx:211-221).
3. *Section writer* (T3/T1): `updateTenderPricingData(…, expectedRevision)` (:919-943); PnlPricingStage threads `tenderRevisionTokenOf(ws)` (PnlPricingStage.tsx:385-391) and keeps the entry on stale.
4. *Drafting write* (T4): `updateTenderDraftingData(…, wsRevisionToken(ws))` from ProposalBlockWorkbenchTab.tsx:702 with honest saved/audit-warning/stale/failed outcomes (:703-710).
5. *Stage mover*: `updateTenderPhase` (:469-490) — value-validation only (`isTenderInternalStageKey`), exact-id store targeting, zero-row honest failure, real actor; no UI token parameter (store in-call token only) — consistent with the mapping register's accepted state for column writers, and stage movement stays free.
- View-model plumbing verified: `bundle.revisionToken` from `header.row.updated_at` (supabase-tender-data.ts:1039-1041) → `bundleToTenderWorkspace` copies it onto ws (:1073) → `wsRevisionToken` accessor (tender-save-outcome.ts:43-46) / `tenderRevisionTokenOf` (IdentifiedStageShared.tsx:109).
- Orchestration guard verified from the diff: `writeOrchestrationState` now selects `updated_at`, refuses without a token, carries `.eq('updated_at', revisionToken)`, and disambiguates zero-row into stale-vs-RLS honestly (orchestration.ts:156-215). Guard-only change.

**5. CORRECTION D — PARTIAL (service layer fully compliant; two call sites drop the warning).**
- `appendConfirmedTenderAudit` is awaited after the confirmed primary write, id-selected-back, 8s timeout, honest reason (supabase-tender-actions.ts:146-179); `savedWithAuditOutcome` maps failure to `saved_with_audit_warning` + `auditWarning` (:185-193). `_insertAuditEvent` is gone (only comments remain, :440,1597-1613). `createActivityNote` confirmed (:1603-1616).
- One deduplicated feed: single `fetchTenderTicketAuditRows` fetch, two projections (supabase-tender-data.ts:946-948 "F5: two projections of ONE deduplicated audit feed — never sum them"); the drawer timeline and all its counters consume only `ws.auditEntries` (TenderGlobalIntelligenceDrawer.tsx:161-190,213).
- Mock-word row-hiding removed from both history tabs; remaining filters are user-selected category/severity only (TenderActivityTab.tsx:56-60; TenderAuditTrailTab.tsx:138-142).
- Residue: the two workspace stage-mover buttons render plain `toast.success("…Persisted to Supabase.")` on `result.success` without surfacing `saved_with_audit_warning` (TenderWorkspace.tsx:2211, 2244) — the only call sites found that drop the warning (all 34 tab files route through the outcome classifiers).

**6. CORRECTION E — CONFIRMED.**
- Upload chain honest at each point: storage+`generated_documents` throw before anything is claimed; step-3 link failure reported as "uploaded_not_linked" with the vault record id and an explicit do-not-re-upload warning (TenderDocumentModal.tsx:96-122, 228-288). Signed-URL failure surfaced (TenderDocumentsLibrary.tsx:54; document-vault.ts:340-343). Stage filter `documentsForTenderStage` (tender-workspace-data.ts:383-385).
- Export renders the edited preview HTML client-side (`buildPreviewHTML(req.blocks)` then native print / pdf-lib merge, final-pack-export.ts:110-173); server route is flag-gated OFF (`VITE_FPS_SERVER_PDF`, server-pdf.ts:43-49) and even when flagged returns null (:56-61) — client fallback always.
- Drift check hashes the same projection: `useSourceDrift` uses the loader's own `TENDER_SOURCE_SELECT` + `buildTenderSourceData` (useSourceDrift.ts:68,83; final-pack-loader.ts:251-285), and `tender_drafting` is inside the hashed projection (:280) — proven by final-pack-tender-mapping.test.ts:158-183 (edited blocks mapped in, `not_captured` stays empty, block edit IS drift).

**7. CORRECTION F — PARTIAL.** Stages verified: 4 (T3 range), 8, 9 (mandated), 13 (T4 late range):
- *Stage 4:* `buildHopPatch` emits only `{hop}` (HOPOperationsModelTab.tsx:146-152); save via patch-merge with threaded token (:192-203); all seven design tabs receive `onSaved: reload` (TenderWorkspace.tsx:2131-2135); load path `type_details.solution_design_data` → `t.solutionDesignData` (supabase-tender-data.ts:682) → tab reads `ws.tender.solutionDesignData.hop` — reload survival closed by code-trace. The manifest's "worst in app" cross-tab clobber is gone.
- *Stage 8:* `decided_by` = session user (ApprovalMatrixStage.tsx:257-261), writes thread `wsRevisionToken` (:213); the meter now reads the canonical matrix via `projectedApprovalMatrix` → `projectTenderStageTruth(...).approval_matrix` (TenderWorkspace.tsx:1186-1194), fixing the manifest's location mismatch; constant-100% governance segments replaced with honest null notes (:1211,1240).
- *Stage 9:* Submission Checklist tab derives ONLY from the recorded `submission_readiness.required_documents` register, honest empty state, full-name/linked-id/status matching — no first-word fuzzy (FinalApprovedStage.tsx:115-142, 805-885); `deriveStageChecklist` reads real keys incl. per-block review truth and canonical matrix (:248-320); `ran_by`/`recorded_by` = session user (:616, :231); AI final check still throws before any write (:38-40, :600). Registers verified in item 2.
- *Stage 13:* all four tabs write `negotiation_data.{negotiation_log,requested_changes,margin_impact,revised_terms}` with `wsRevisionToken` (NegotiationLogTab.tsx:85; NegotiationChangesTab.tsx:105; NegotiationMarginTab.tsx:64; NegotiationRevisedTermsTab.tsx:92); the meter reads exactly those keys (TenderWorkspace.tsx:1042-1087); reload survival via `ws.tender.typeDetails` (supabase-tender-data.ts:685).
- Stage-6 meter key mismatches from the manifest are fixed (`compliance_coverage.requirements`, `appendices_evidence.evidence_gaps` — TenderWorkspace.tsx:767-790).
- **Miss found (defect 1):** the stage-9 sidebar meter's "Stage Checklist" segment still derives its internal-review item from the phantom `tender_drafting.departmental_reviews` facet — see defects.

**8. ISOLATION — CONFIRMED.** No old-app imports added anywhere in `git diff 93799d3..HEAD` (zero hits). No legacy tender child-table writes: write-target census over the tender surface shows only `commercial_tickets`/`commercial_ticket_audit`/doc tables; the sole `tender_*` access is the pre-existing read-only `tender_governance_config` fetch with writes explicitly refused (supabase-governance-data.ts:230-245, untouched by the wave). No gates: `updateTenderPhase`/`updateTenderCrmStage` contain only value validation (supabase-tender-actions.ts:475-477, 511-518); the single stage-move UI is a free confirm dialog with no readiness pre-checks (TenderWorkspace.tsx:2239-2250). No AI activation: `generateBlockContent`/`generateDocumentContent` still throw (ai-runs.ts:445-469, file untouched — empty diff); component-local shims throw (FinalApprovedStage.tsx:38-40; DepartmentalReviewTab.tsx:32-34) and the drafting controls carry honest Sprint-X refusal labels.

**9. SCOPE — CONFIRMED.** Every diff file (list re-derived) is tender-surface, `docs/tender-closure`, or lane tests, plus: PdfStudio.tsx and useFinalPackInstance.ts — verified **actor-label-only** diffs ("User" → "Unauthenticated"/session actor; 4 hunks + 1 hunk, nothing else); and two lib files beyond the two named grants: `orchestration.ts` (guard-only, mandated by pin P2c) and `tender-stage-source-truth.ts` (10-line additive P1 registration of `submission_readiness` into stage truth — diff verified confined). Nothing touches Proposal/Renewals/System behaviour. The grant lists were incomplete rather than violated; noting for the record.

**10. TEST QUALITY — CONFIRMED (no placebo).** Inspected deeply:
- `src/lib/tender-facet-writers.test.ts` (T1): stateful commercial_tickets mock that **enforces** the `.eq('updated_at')` predicate and column projections like PostgREST; asserts exact filters `[id, ticket_type, active, updated_at]`, sibling-key preservation per all 7 writers, stale = zero DB calls, zero-row honest failure, audit-warning path, actor truth, CRM refusal-before-read.
- `src/components/tender/tcw-t3-tab-payloads.test.ts` (T3): key-exactness on all 11 patch builders (named GUARD test on `buildHopPatch`), end-to-end tab-shaped save with sibling survival, stale non-destructive with `onConfirmed` spy never firing.
- `src/components/tender/submission-readiness-tabs.test.tsx` (T5): three-state load honesty, per-item exact-id + expectedRevision spying, amber-not-green audit warning, exact-id link matching with an explicit no-first-word-fallback case, rendered-markup honesty. Real assertions throughout; failure paths covered in all three.

**11. LIVE READ-ONLY PROBES — CONFIRMED, zero live writes during the wave.**
- Census: `Content-Range: 0-1/2` — exactly the KAFD proposal `089447d6-…` and the Linde tender `7483c493-0098-40a9-9e5f-76007bc62cd1`, both active.
- Linde `updated_at` = `2026-08-19T09:38:11.074248+00:00` — **byte-identical** to the pre-build value recorded in TENDER-DATA-CONTRACT-REGISTER.md §6.
- Linde audit rows: `Content-Range: 0-193/194` — **exactly 194**, no delta.
- Linde `type_details` top-level keys: **16 keys, identical to the register's recorded list** (no `submission_readiness`, `orchestration`, `approval_matrix`, `final_approved`, or `documents` appeared).
- Legacy child tables all `*/0` to anon (tenders, tender_packs, tender_placeholders, tender_required_documents, tender_compliance_items, tender_activity_events, tender_audit_events, tender_submission_gates, tender_pack_outputs, tender_submission_emails).

**12. WORST FINDING — the stage-9 sidebar meter still reads a facet nothing writes** (defect 1 below). Other attack lines that did NOT break: no direct `commercial_tickets` writes outside the guarded store + orchestration; no surviving "Current User"/"System"/"Resubmitted by drafter" actor literals; `ws.tender.typeDetails` carries full type_details so every late-stage tab's read survives reload; the clarification two-write pair is sequenced with honest partial outcome (IdentifiedClarificationLogTab.tsx:87-134); the TOC second write is checked (ProposalArchitectureTOCTab.tsx:679-696); drawer note capture is confirmed + reloads (TenderGlobalIntelligenceDrawer.tsx:200-209); readiness header says "not measured (no packs configured)" (TenderWorkspace.tsx:1737); Submitted stage editable views have real dirty wiring (SubmittedStage.tsx:89-146).

## Defect list

1. **MAJOR (mandated-correction miss; low operational impact — meter understates, never overstates).** Stage-9 sidebar "Stage Checklist" meter derives its internal-review item from the phantom `tender_drafting.departmental_reviews` facet, which **no code path writes** — `TenderWorkspace.tsx:1347-1348` (`reviews[d]?.submitted_at` → `reviewDone`). Every other reader of this orphan was retired with comments (FinalApprovedStage.tsx:159,296,522; PreviousStageIntelligence.tsx:525; TenderGlobalIntelligenceDrawer.tsx:543), and the stage-9 TAB checklist was fixed to derive from per-block review statuses (FinalApprovedStage.tsx:294-305) — so the meter permanently caps at 6/7 ≈ 86% and disagrees with its own tab even when all real checks pass. The new stage-meters test suite does not cover this segment, so it escaped both fix and tests. Failure scenario: fully reviewed + fully approved tender shows an incomplete Stage-9 checklist meter forever.
2. **MINOR.** Global Intelligence drawer Deadline tile fabricates a verdict from absent data: with no submission deadline captured, `submissionDeadline` is `''` (supabase-tender-data.ts:653) → `daysLeft = NaN` (TenderWorkspace.tsx:1678, unguarded) → both the value and the sub-label render **"Overdue"** (TenderGlobalIntelligenceDrawer.tsx:373,377). Pre-existing at baseline; survived the Correction-B sweep that fixed the adjacent tiles (readiness/submission/compliance tiles are guarded, :345,404,417).
3. **MINOR.** The two stage-mover call sites drop the P3 audit warning: `saved_with_audit_warning` renders as plain green "Persisted to Supabase." (TenderWorkspace.tsx:2211 CRM move, :2244 internal-stage move). Primary save is real; only the advisory-history warning is silently lost, contrary to the "never silent" contract honoured everywhere else.
4. **MINOR (dead code / resurrection risk).** The `buildStageConfig` indicators array is dormant (only `.tabs` is consumed — TenderWorkspace.tsx:1597-1601), but inside it the census-named B7 gauges survive un-derived: "Strategic Fit" = pack `compliancePct` (tender-stage-config.ts:177), "Feasibility" (:191), "Scope Clarity" = pack `docsPct` (:192) — permanently 0 because `fetchTenderPacks` returns `[]` (supabase-tender-data.ts:743-746) — plus the `daysLeft = 999` sentinel (:88-90). Pin P5 hedged between deriving and deleting the array; the lanes did neither for these entries.
5. **COSMETIC.** InternalReviewExceptionsTab.tsx:82 shows Status "All Clear" when zero rejected entries exist, including when no review was ever recorded (derived from real block statuses and scoped to an exceptions register, so defensible). Drawer Margin-Health tile shows "Review" when target GP is simply not captured (TenderGlobalIntelligenceDrawer.tsx ~:395; pre-existing).
6. **OBSERVATION (design-accepted).** T3's stale-retry arming deliberately bypasses the UI token on the retry save (IdentifiedStageShared.tsx:151-152) without refreshing the on-screen data first; the patch-merge confines any overwrite to the tab's own keys, and the T5 register tabs do refresh on stale. The stale toast's "review the current value" wording slightly overpromises on the T3 path. Also: register read-back comparison checks id+status+name only, not all fields (supabase-tender-actions.ts:1358-1362); column writers (phase/CRM/probability/team) accept no UI-load token (store in-call token only) — matches the mapping register's recorded contract.
7. **OBSERVATION (process).** Commit `9862eab` (evidence docs) was pushed into the tree at 11:12 while this audit was in progress. Docs-only; no impact on findings — but the audit window was not quiescent.

## OVERALL VERDICT: FINDINGS
- 1 major: stage-9 sidebar checklist meter reads the never-written `departmental_reviews` facet (TenderWorkspace.tsx:1347-1348) — the one surviving "meter still lying" instance, in the conservative direction.
- 2 minor honesty residues: drawer Deadline "Overdue" fabricated from an absent deadline; stage-mover toasts drop the audit warning.
- 1 minor dead-code residue (dormant B7 gauges + 999-day sentinel) and cosmetics/observations as listed.
- **Zero release-blocking findings.** All 12 checklist items otherwise held under adversarial re-derivation: gates reproduce exactly (tsc 0 · 943/943 across 67 files · 2,494 modules), the three registers are real and reachable, the save contract (exact-id, revision threading, stale-non-destructive, zero-row honesty, sibling preservation, real actors) holds on every writer sampled, audit is awaited-and-surfaced at the service layer, the documents/FinalPack chain is honest with the server PDF route off, isolation is clean, scope is in-grant (two pin-mandated lib files noted), the new tests genuinely enforce the contracts, and the live DB shows zero writes during the wave (census 2, audit 194, 16 unchanged type_details keys, legacy tables 0).
