# 07 — DEFECT AND GAP REGISTER
**Audit:** PDF Studio Commercial-Grade Audit · **Baseline:** `proposal-functional-closure` @ `e0c7062` (tag `proposal-functional-closure-complete`) · **Date:** 2026-08-24
**Method:** 11 parallel read-only inspection lanes + orchestrator browser walkthrough (authenticated, localhost:5310, read-only). Cross-lane duplicates merged; lane IDs kept as cross-references. Severity ranked. "Browser" column = live-verified in the authenticated walkthrough vs source-only.

Classification legend: defect / missing capability / misleading claim / data-contract gap / clutter / not tested.

## BLOCKER — puts wrong or internal content into a real customer document

| ID | Sev | Route/workflow | Evidence | Current behavior | Expected | Class | Blocks commercial use | Browser | Treatment |
|---|---|---|---|---|---|---|---|---|---|
| PDS-01 (TSRC-06) | BLOCKER | Every customer-facing pack with pricing blocks | `final-pack-loader.ts:846-855`; `final-pack-preview.ts:401-414` | Internal `cost`, `gp_percent`, `recommended`, internal scenario `notes` render into the exported customer pricing tables | Customer documents show customer pricing only — never internal margin | defect (internal-data exposure) | **YES** | source-verified (no priced pack live to view) | Customer-facing pricing projection without internal columns |
| PDS-02 (PREV-01 / TSRC-04) | BLOCKER | 4 of 5 pack templates (terms/legal blocks) | `final-pack-loader.ts:901-906` (clauses fetched); `final-pack-preview.ts:360-369` + `BlockEditor.tsx:64-77` (never rendered/edited) | Published clause-library content (EN+AR) is loaded into `content.clauses` and **never rendered**; the block ships the seeded meta-boilerplate "Terms are loaded from the Clause Library. Add specific clauses via the Template Builder." marked "populated" | T&C/legal blocks render the actual clause text | defect (silent content drop) | **YES** | **CONFIRMED LIVE** — boilerplate present in the Linde and KAFD pack previews | Add a `clauses` renderer branch + clause-aware editor |
| PDS-03 (PREV-03) | BLOCKER | Confidentiality block, 4 of 5 templates | `final-pack-loader.ts:701-713` captures the variable; no interpolation in `final-pack-preview.ts`; `standalone-template-source.ts:152-161` lacks `recipient_name` | Literal `{{recipient_name}}` prints in preview and export | Resolved customer name | defect (broken variable pipe) | **YES** | **CONFIRMED LIVE** in both existing packs' previews | Interpolate block variables in `renderHtmlBlock` |
| PDS-04 (TSRC-02) | BLOCKER | SLA pack (tpl-003, required block) | `final-pack-loader.ts:961-966` reads `kpi/measurement/penalty`; writer produces `kpi_name`/`measurement_method`, no penalty (`SLAKPIModelTab.tsx:45-48,65-71`) | SLA matrix renders "Not captured yet" in 3 of 4 columns despite fully captured KPIs, while `source_status` claims "populated" | Captured KPI rows appear in full, or honest not-captured | data-contract gap | **YES** (SLA pack) | source-verified | Map the real field names; decide penalty source honestly |
| PDS-05 (ROUTE-01 / TSRC-01 / CLUT-01 / PERS-04) | BLOCKER | Drift banner, both connected routes | `SourceDriftBanner.tsx:89`; `PdfStudio.tsx:641-643`; `useFinalPackBlocks.ts:19` ("Does NOT re-read commercial_tickets") | "Tender source has changed" banner offers **"Refresh from source"**, which restores the frozen creation-time snapshot, discards user edits, never fetches current source, never updates `_hash` — banner persists and the user believes they refreshed | Refresh pulls current source, or the button says "Restore original snapshot" | misleading claim + defect | **YES** (user can send a pack believing it reflects the updated tender) | **banner observed live** on the Linde pack (source genuinely changed 2026-08-19; button not clicked — read-only) | Rewire to re-run `loadTenderPack` + update hash, or relabel |
| PDS-06 (TSRC-07 / PSRC-01) | BLOCKER | Drafting → pack, tender AND proposal | `final-pack-loader.ts:715-774` (5 title-heuristic slots, first-match-only); `ProposalBlockWorkbenchTab.tsx:44-92` (18 structured block types ignored) | Drafted blocks reach the pack only if titles match 5 magic phrases ("introduction", "scope of service/work", "closing", "annexure…config", "communication"); everything else (Operating Model, Implementation Plan, Manpower, Risk…) is **silently dropped** and replaced by default prose; no warning is pushed | Everything the drafter wrote lands in the pack or the omission is disclosed | missing capability + silent data loss | **YES** for full-proposal packs | source-verified | Match on `block_type`/`document_assembly_target`; ingest unmatched blocks as sections; add loader warnings |

## HIGH

| ID | Sev | Route/workflow | Evidence | Current behavior | Expected | Class | Blocks | Browser | Treatment |
|---|---|---|---|---|---|---|---|---|---|
| PDS-07 (TSRC-08 / PSRC-03) | HIGH | Pricing commercial terms → packs; Quote stage → quotation packs | `tender-pricing-types.ts:174-241` (rows carry `include_in_proposal`); no loader reader; no `workspace.quote`/`commercialTerms` read (`final-pack-loader.ts:292-340`) | Payment terms, VAT %/treatment, validity, surcharges, exclusions, assumptions — flagged for inclusion — never reach any pack; Quote-stage content never reaches the quotation packs | Rows flagged for the proposal appear in it; quotations reflect the recorded quote | missing capability | Yes (quotation credibility) | source | Commercial-terms + quote-stage resolvers |
| PDS-08 (TSRC-05 / PREV-04) | HIGH | Bilingual Quotation (EN/AR) pack | `final-pack-preview.ts:202` (`lang="en"` hardcoded), `:241-246`; zero `dir`/RTL/Arabic fonts; `content_ar` loaded never rendered | "Bilingual quotation with VAT breakdown — English and Arabic" renders English-only, LTR, no VAT | Pack matches its label, or the label states reality | misleading claim | Yes for AR-required customers | card seen live | Implement EN/AR+VAT rendering or relabel the card |
| PDS-09 (TSRC-11) | HIGH | Totals block, all packs | `final-pack-loader.ts:888-897` (`parseFloat` on free-text revenue) | "1,200,000" → "SAR 1.00" / "One Saudi Riyals" in the exported document | Total equals scenario revenue or states unparsable | defect | Yes when it fires | source | Locale-tolerant parse + honest unparsable state |
| PDS-10 (EDIT-01 / ROUTE-08 / CLUT-02) | HIGH | Composer → Reset dropdown | `PdfStudio.tsx:534-540` (comment admits "Reset first visible block as example"); `UndoRedoToolbar.tsx:99,118-124` | "Reset selected block from source" always resets `blocks[0]` (the cover); no selection mechanism exists; confirm dialog reinforces the false claim | Reset the block the user chose, or remove the item | defect + misleading claim | Yes (destructive action on wrong target; undo mitigates) | control seen live | Per-card reset or remove menu item |
| PDS-11 (EDIT-02) | HIGH | Composer → undo/redo/reset/restore with an expanded rich-text block | `NarrativeEditor.tsx:32-47`, `CustomTextEditor.tsx:31-46` (content read at mount only); contrast `BlockCard.tsx:175-178` (AI path re-mounts deliberately) | Toolbar Undo (or Reset/version Restore/"Reload latest") does not update an open TipTap editor; the next keystroke silently re-saves the pre-undo text, reverting the undo; ~50 keystrokes evict all structural history (one stack entry per keystroke) | On-screen editor reflects the undone state | defect (silent content reversion) | Yes | source (interaction not exercised — read-only) | Re-mount/re-seed expanded editors on external content change |
| PDS-12 (EXP-01 / PREV-08) | HIGH | Any PDF export of a doc with imported-PDF cover | `final-pack-pdf.ts:71-80` (`allowTextFallback` default true), `:129-177` (jsPDF text renderer); `final-pack-export.ts:141-144` (same `renderer:"client-pdf-merge"` either way) | On html2canvas failure the body silently degrades to text-only jsPDF (branding, colors, images, table structure gone); UI and audit report it identically to the high-fidelity render | User is told which renderer produced the file | silent fidelity degradation | Yes (unstyled pack can ship) | source | Distinct renderer value + outcome text naming the fallback |
| PDS-13 (PERS-01) | HIGH | Composer → change branding → keep editing | `PdfStudio.tsx:387-398` (branding write bumps `updated_at`, never syncs `lastUpdatedAtRef`) vs `:191-201` | Next autosave's token guard matches 0 rows → **false** "Another session saved changes" banner; "Reload latest" then replaces the user's unsaved edits with DB state while claiming "Your edits are kept locally" | Self-writes never fabricate a conflict | defect | Yes (misleads users into discarding their own work) | source | Sync token after self-writes, or route branding through the guarded save |
| PDS-14 (DOC-01) | HIGH | Tender upload, step-3 (register link) failure | `TenderDocumentModal.tsx:74-80`; `document-vault.ts:224-226`; `document-runtime.ts:248-250` | The orphaned vault record (storage + vault row, no register entry) appears in **no UI surface**; toast says "check the library" but the library reads the register only; recovery is manual DB work | A "in vault but not linked" view + relink | missing capability | Yes (stored customer file unreachable) | source | Vault-vs-register reconciliation view / relink action |
| PDS-15 (DOC-02) | HIGH | Proposal stage Supporting Documents → download | `SupportingDocumentsPanel.tsx:301-339` (`filePath:null, versions:[]`); `document-vault.ts:631-642` (silent console.warn no-op) | Clicking "Click to download" on any proposal supporting-document row does nothing visible — always | Download or an error | defect | Yes (proposal evidence retrieval broken) | source | Carry `storage_path` through; toast on failure |
| PDS-16 (PREV-02) | HIGH | Connected packs, un-captured narrative sections | `final-pack-preview.ts:360-369` (default_content short-circuits the `not_captured` check) | Marketing boilerplate ("pleased to present…", scope bullet lists) renders unmarked as customer content; the honest "Content not captured yet." branch is unreachable for every seeded block | Un-captured sections visibly say so, or defaults are labeled | honesty gap | Yes (misleading exported prose) | **partially confirmed live** (terms boilerplate in both packs; intro in Linde was genuinely captured) | Check `source_status==="not_captured"` before falling back to defaults |

## MEDIUM

| ID | Sev | Route/workflow | Evidence | Current behavior | Class | Browser | Treatment |
|---|---|---|---|---|---|---|---|
| PDS-17 (ROUTE-02) | MEDIUM | URL param change while composer open | `PdfStudio.tsx:83-87` (no reset effect); contrast `useTenderWorkspaceData.ts:57-65` (W04-T08-A guard) | Ticket A's composer survives a param change to ticket B: A's document under B's URL; drift compares B against A's hash → fabricated drift; exports run against A | defect | not exercised | Reset mode/activeInstance on `sourceTicketId` change |
| PDS-18 (ROUTE-03 / PSRC-05 / CLUT-07) | MEDIUM | Proposal-created instances | `useFinalPackInstance.ts:167-169`; `document-source.ts:170` | Stored `linked_entity_type='tender'` for proposal-sourced docs; resume works only via the same mislabel | data-contract gap | source | Thread `sourceKind` into snapshot/createInstance/list |
| PDS-19 (ROUTE-04 / TSRC-10) | MEDIUM | `/pdf-studio` → Resume → connected doc | `PdfStudio.tsx:254-260` (hook keyed to URL param); `useSourceDrift.ts:108-113` | Drift silently never checked (no banner — indistinguishable from "checked, unchanged") | defect (silent unknown-as-clean) | source | Key drift to instance `linked_entity_id` |
| PDS-20 (PSRC-02) | MEDIUM | Proposal → PackSelector scenario picker | `PackSelector.tsx:137-153` (raw `td.pricing`, not the normalized projection) | Proposal P&L versions never appear; banner claims pricing will be empty while the created pack populates from the working P&L anyway; no scenario choice for multi-version proposals | dishonest empty-state + lost choice | live view was truthful for KAFD (0 P&L versions) — defect triggers only when versions exist | Normalize before extracting scenarios |
| PDS-21 (TSRC-03) | MEDIUM | scope.table block | `final-pack-loader.ts:776-780` reads `scope_items`/`deliverables`; no writer produces them (`sow-data-types.ts:57-97`) | Dead mapping — SOW capture never reaches the pack; block always "not_captured" (not in seeded recipes; Add-Block only) | data-contract gap | source | Map real SowData fields or retire block |
| PDS-22 (TSRC-12) | MEDIUM | Annexure C — Rate Card | `final-pack-loader.ts:659-660,812-857` | "Rate Card" renders the scenario P&L summary (incl. cost/GP → compounds PDS-01), never rates; `cost_inputs` never ingested | misleading claim | source | Rate-card resolver or rename |
| PDS-23 (TSRC-09) | MEDIUM | Documents → pack | `final-pack-loader.ts:270-285`; no resolver | Uploaded tender/proposal documents never reach any pack block (evidence/annexure); also absent from the drift hash | missing capability | source | Evidence/annexure ingestion of active documents |
| PDS-24 (PSRC-04) | MEDIUM | Proposal → SLA/scope blocks | `final-pack-loader.ts:776-794,944-968` (tender-shaped top-level keys proposals never write) | SLA pack from a proposal contains only party details + boilerplate; scope table always empty (honest empty-state, so not deceptive) | ingestion gap | source | solution_design projections for proposals |
| PDS-25 (DOC-03) | MEDIUM | Final export → document record | `final-pack-export.ts:103-219` (no vault write); `server/routes/documents.ts:428` (501 text claims "the client persists it via the documents vault" — false) | Exported finals exist only in Downloads; no `generated_documents` row, no register entry — no retrievable record of what was sent | missing capability + inaccurate server message | source | Post-export "save to vault" via existing writer; fix 501 text |
| PDS-26 (DOC-04) | MEDIUM | Register archive | `supabase-tender-actions.ts:1281-1283` | Archiving overwrites `document_category` with "Archived" — original category destroyed; no register-side restore | defect | source | Separate archived flag |
| PDS-27 (DOC-05) | MEDIUM | Post-upload metadata edits | Register edits S3-only (`supabase-tender-actions.ts:1215-1266`); vault edits S2-only, fire-and-forget (`document-vault.ts:575-583`); vault dialog offers name/tags with no columns (`DocumentViewer.tsx:692-696`) | Dual stores drift silently; vault name/tag "saves" report success and persist nothing | data-contract gap | source | Persist only real columns; one metadata master |
| PDS-28 (DOC-07) | MEDIUM | Archived docs as evidence | `TenderDocumentDrawer.tsx:104-117` (unfiltered buckets); `TenderRequiredDocumentsTab.tsx:417` | Archived documents still appear in drawer buckets and satisfy required-document links with no marker | defect | source | Filter/badge archived |
| PDS-29 (EXP-02) | MEDIUM | Imported-cover merge fails → print fallback | `final-pack-export.ts:145-151`; placeholder text `final-pack-preview.ts:295-305` | Printed page 1 is a card claiming "Used as static page 1 in PDF export" — false for that artifact | misleading artifact content | source | Fallback-aware placeholder text |
| PDS-30 (EXP-03 / PREV-11) | MEDIUM | Branding footer options | `final-pack-preview.ts:36-42` vs `:189-198` | `show_page_numbers` and `show_completed_by` are dead — no export path renders page numbers (tender compliance risk) | unimplemented declared feature | source | Implement or remove flags |
| PDS-31 (EXP-04) | MEDIUM | Draft/Test html2pdf download | fixed watermark rasterized once (`final-pack-preview.ts:722-748` + `final-pack-pdf.ts:103-112`) | DRAFT/TEST watermark + footer appear on page 1 only in multi-page direct downloads (print path repeats per page) | watermark divergence | source | Per-page stamp in raster path |
| PDS-32 (EXP-07 / PREV-07) | MEDIUM | Export font/image readiness | `final-pack-pdf.ts:98` (250ms), `final-pack-export.ts:281-282` (800ms), fonts via CDN `@import` (`final-pack-preview.ts:541`) | No `document.fonts.ready`/image-decode gate — exported PDF can silently use fallback fonts or miss slow images | nondeterministic fidelity | source | Await readiness before capture/print |
| PDS-33 (UX-01) | MEDIUM | Composer layout in shell | `PdfStudio.tsx:463` (`h-screen`) inside `CleanLayout.tsx:244-249` | Double vertical scroll; export toolbar region below the fold on load; "pinned" bars pseudo-pinned | defect | **CONFIRMED LIVE**: main scrollHeight−clientHeight = 104px in the open composer | Size studio to the layout slot |
| PDS-34 (UX-03) | MEDIUM | Preview geometry | `.fps-preview-iframe` `width:595px` but flex-shrink (`final-pack-tokens.css:160-178`) | Preview page shrinks with the pane — line wraps/page fill differ from the print engine | defect (undermines WYSIWYG trust) | **CONFIRMED LIVE**: iframe offsetWidth = 507px (not 595) | `flex-shrink:0` + scale-to-fit |
| PDS-35 (UX-02 / UX-11) | MEDIUM | Composer below ~768px width | Zero media queries in composer path (grep-verified); computed ~38px editor at 390px; 22–28px touch targets | Desktop-only composer; mobile unusable | missing capability | NOT TESTED live (window maximized — resize refused); computed | One breakpoint: stack panes; touch-target pass |
| PDS-36 (UX-06 / UX-07 / UX-08) | MEDIUM | Studio dialogs + a11y | No `role="dialog"`/Esc/focus-trap on 5 dialogs; ~30 title-only icon buttons; 5 close buttons with no accessible name; resizer not keyboard-reachable | Keyboard/AT users blocked from dialogs and resizer | missing capability | source | Standard dialog primitive; aria-label pass (RichTextToolbar is the in-repo model — 19/19 labeled) |
| PDS-37 (EDIT-04) | MEDIUM | Pricing/SLA editors | `PricingEditor.tsx:43-108`, `SlaEditor.tsx:24-97` | Cell edits only — no add/remove row; standalone docs cannot build a priced table except via custom text | missing capability | source | Row add/remove controls |
| PDS-38 (EDIT-09) | MEDIUM | Volumes | frozen `doc_template_volumes` in snapshot; `VolumeSelector.tsx:25-28` strict `block_keys` filter | Human-added blocks match no volume — silently excluded from per-volume and All-Volumes exports | missing capability | source | Editable membership or "not in any volume" indicator |
| PDS-39 (CLUT-06) | MEDIUM | ProposalWorkspace "Generated Documents" card | `WorkspaceDocumentSection.tsx:2-3,95,102`; server route always 501 | "Server PDF" badge + "Generate Final PDF" buttons on a route that always refuses (honest toast after click); stale "Sprint 6… fully audited" comment | misleading claim | source | Relabel; state up front |
| PDS-40 (CLUT-04) | MEDIUM | AI sparkle menus | `final-pack-bots.ts:247-259`; `BlockAISparkleMenu.tsx:89-91`; runtime refusal `final-pack-bot-runtime.ts:74-89` | Bots listed as runnable although every run refuses ("deferred to Sprint X"); user learns only after selecting. Refusal itself is honest; discovery is real Bot Builder data; no governance mock in this repo | misleading at listing | sparkle controls seen live | State AI unavailability at menu level (execution itself is Sprint-X-deferred, listed in doc 10) |
| PDS-41 (CLUT-08) | MEDIUM | Template-created standalone docs | `standalone-template-source.ts:152-172`; `TemplatePicker.tsx:84-90`; `findUnresolvedVariables` exported, never called | `{{customer_name}}`/`{{reference_number}}` tokens render literally; WarningBanner never flags them | clutter + missing capability | source | Use `findUnresolvedVariables` in WarningBanner; optional fields at creation |
| PDS-42 (PERS-05) | MEDIUM | Instance status model | `useFinalPackInstance.ts:28` (draft\|compiled\|exported typed); `updateStatus` zero callers; export never transitions | Every instance is "draft" forever, including exported finals; badge implies a live lifecycle | misleading claim | **CONFIRMED LIVE**: all 4 existing instances show `draft` | Truthful transition on final export, or remove badge |

## LOW / CLUTTER (condensed — full lane detail in the lane reports)

| ID | Route | Finding | Class | Browser |
|---|---|---|---|---|
| PDS-43 (PSRC-06 / CLUT-03 / ROUTE-07) | proposal route | 6+ unconditional "tender" strings on the proposal path: "Loading tender data…" (`PackSelector.tsx:225`), "Could not load this tender" (:238), loader error `Tender ${id} not found` (:414), drift banner ×3 (`SourceDriftBanner.tsx:52,73`; `useSourceDrift.ts:79`), "not linked to tender source" (`CustomTextEditor.tsx:55`); snapshot field names `tender_id/tender_title`. Conditional branches (`not found`, pricing banner, back-link) are correct | clutter/misleading | proposal-conditional pricing banner confirmed live |
| PDS-44 (EDIT-03 / UX-05 / CLUT-05) | composer | Drag handle with grab cursor; no DnD wired anywhere (`BlockCard.tsx:230-232`; no dnd-kit imports) — inherited from old app | false affordance | handles seen live |
| PDS-45 (EXP-05) | exports | Filenames: no customer/ref; Arabic titles collapse to `_`; same-day collisions (`final-pack-export.ts:456-468`) | gap | source |
| PDS-46 (EXP-06) | exports | Print + single HTML hardwired `draft` (watermarked); All-Volumes hardwired `final` (never watermarked) — inconsistent | gap | source |
| PDS-47 (PREV-05 / EDIT-08) | Facility Gallery | "Gallery" is a 3-row text table; no images anywhere; `max_images` dead; always injects company defaults (`source_status:"default"` — WarningBanner does count it) | stub presented as feature | block seen live, "asset" chip |
| PDS-48 (PREV-09) | HTML export | Embedded 3600s signed URLs — images break after an hour | time-bombed artifact | source |
| PDS-49 (PREV-10 / CLUT-09) | preview/branding | Silent image drop on unsignable URL; branding read failure rendered as "No branding profiles" (failed-read-as-empty — the pattern W04-T09 fixed elsewhere) | defect | source |
| PDS-50 (PREV-12) | preview | Hardcoded `exportMode="draft"` — final (unwatermarked) look never previewable | gap (errs honest) | DRAFT watermark seen live |
| PDS-51 (PERS-03) | doc_instances | No DB `updated_at` trigger (reverified in DDL); revision token client-authored, convention-only | data-contract gap | source |
| PDS-52 (PERS-06) | versions | `version_number = max+1` client-side; unique constraint unverifiable; concurrent tabs can duplicate | gap | source |
| PDS-53 (PERS-07) | History → Restore | Restores blocks only; branding/layout/volumes/snapshot not recovered despite FPS-007 migration intent | gap | History button seen live |
| PDS-54 (PERS-08 / CLUT-10) | attribution | `last_edited_by` never written anywhere; `saveRecipeVersion` hardcodes `created_by:"User"` (`useTemplates.ts:282`) — fabricated actor vs the repo's own P4 rule | gap + misleading | source |
| PDS-55 (PERS-09) | create timeout | 20s timeout may orphan a landed insert; retry mints a new UUID | edge defect | source |
| PDS-56 (PERS-02 / DOC-11) | dead code | Unused `saveInstance`/`updateBlocks`/`updateStatus`/`updateBranding` (no token, no read-back, console-only errors); `supabase-sync.ts:336-475` legacy doc-instance sync targets wrong table names (`compiled_documents` vs `doc_compiled_outputs`) — zero callers | latent defective duplicate + clutter | source |
| PDS-57 (ROUTE-05) | standalone blank | "Reference number (optional)" collected and silently discarded (`standalone-blank-source.ts` never reads `refNumber`) | defect | field exists live |
| PDS-58 (ROUTE-06) | hand-typed URLs | No `ticket_type` check against route kind — `/tenders/<proposal-id>/final-pack` opens mislabeled | gap | not exercised |
| PDS-59 (TSRC-13) | drift scope | Hash includes never-ingested fields (`sow_data`, `estimated_value`…) → drift can flag changes the pack can't reflect; `documents` changes never flag | gap | Linde banner live (cause not decomposed) |
| PDS-60 (TSRC-14 / PSRC-09) | cover | Missing ref → UUID prefix as customer-facing "Ref:"; missing date → today | clutter | **CONFIRMED LIVE**: "Ref: 7483c493" / "Ref: 09f1e3fd" style covers |
| PDS-61 (DOC-06) | vault viewer | Missing storage object → viewer asserts "not a missing file" (false); download silently no-ops | defect | source |
| PDS-62 (DOC-08 / O2) | cover assets | Replacing/removing a cover strands the old private-bucket object; upload-only module, no cleanup surface (matches FPS-009 backlog) | gap | source |
| PDS-63 (DOC-09) | storage paths | Tender uploads land under `customers/unknown/workspaces/unassigned/` — path encodes neither tender nor (sometimes) customer | gap | consistent with FPS-009 records |
| PDS-64 (DOC-10) | proposal docs tab | Loading state rendered as "No documents yet" (sync read of async registry) | defect | source |
| PDS-65 (UX-04/09/10/12–17) | composer polish | Unclamped persisted split; non-wrapping header (clips below ~1100px); 61 sub-12px text instances; up to 4 nested scrolls (3 measured live); no overflow-wrap in doc body; undo buttons not `disabled`; `window.prompt` for links; dynamic Tailwind class luck (`PricingEditor.tsx:165`) | defects/clutter | 3 scroll containers measured live |
| PDS-66 (CLUT-11) | Cover Designer | "Image cover" mode persists a value nothing reads (only `imported_pdf` consumed) | clutter | source |
| PDS-67 (CLUT-12/13) | toolbar | Undo/Redo clickable when unavailable; green ✓ shown for print-dialog-only exports (outcome line is precise) | clutter | source |
| PDS-68 (CLUT-14) | naming | Three names for one surface: "PDF Studio" (nav), "Final Pack Studio" (header), "Document Studio" (standalone label) | clutter | **all three seen live** |
| PDS-69 (CLUT-15/16) | standalone entry | "Duplicate Existing Document — Coming soon" disabled stub (honest); connected picker tender-only with no note | disclosed stub / gap | **seen live** |
| PDS-70 (EDIT-10) | template dialogs | Stale-closure error messages show generic instead of specific reason on first failure | cosmetic | source |
| PDS-71 (EDIT-06 / UX-14) | undo | No keyboard binding for block-level undo; two rival undo systems (TipTap internal vs block stack) | gap | source |
| PDS-72 (walkthrough) | resume lists | Instances with empty `customer_name` render as "— Full Commercial Proposal" (leading em-dash label) | cosmetic | **CONFIRMED LIVE** (2 of 4 instances) |
| PDS-73 (AUTO-01/02/04/05, EXP-09/10) | tests/build | 800ms print timer never cleared (flake vector — did NOT appear in 3 measured runs); 5 chunks >500kB (PdfStudio 636kB, html2pdf 976kB); zero tests for the real PDF renderer, popup-blocked branch, autosave, undo/redo, block editors (20 components untested); `final-pack-pdf.ts:8` claims "unit-tested" — false | hygiene/coverage | measured |

## NOT TESTED (honest limits)
| Item | Reason |
|---|---|
| Exact 1366×768 and 390×844 viewports | The browser window is maximized; the extension's resize was accepted but the OS did not apply it (innerWidth stayed 1536). Desktop measurements at 1536×839 were captured instead; mobile behavior is computed-only (UX lane predictions). |
| Export button clicks (Draft/Test/Final/Print/HTML) | Read-only ruling — exports write `doc_compiled_outputs` audit rows. Export truth rests on source + the 9 passing export tests + the Tender-wave live export evidence (2026-08-21, same pipeline). |
| Editor interactions (typing, reorder, undo, reset, drift refresh click) | Read-only ruling — all mutate `doc_instances`. |
| Pack creation from either connected route or standalone | Read-only ruling. |
| Live `doc_block_library`/`doc_templates` row contents | Authenticated-only tables; default-prose census derived from the seed SQL the live DB inherited. |
| Wrong-ticket param-change leak (PDS-17) | Requires navigation gymnastics on live packs; inferred from wouter semantics + absent guard. |

## Sprint X deferred observations
Collected without solutions in [10-SPRINT-X-DEFERRED-OBSERVATIONS.md](10-SPRINT-X-DEFERRED-OBSERVATIONS.md).
